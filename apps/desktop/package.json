{
  "name": "@mythos/desktop",
  "version": "0.2.0-beta.4",
  "private": true,
  "type": "module",
  "main": "src/bootstrap.cjs",
  "scripts": {
    "dev": "electron .",
    "pack:config": "node scripts/pack-hot-config.mjs",
    "pack:mac": "node scripts/prepare-runtime.mjs && electron-builder --mac dir dmg zip --arm64 --config electron-builder.config.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "^26.0.12"
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  }
}
