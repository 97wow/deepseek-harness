# 进度看板

最后核验：2026-08-25。

状态定义：`待办`、`进行中`、`阻塞`、`完成`、`取消`。“完成”表示代码与非付费验证已存在；需要真实模型 cohort、签名或远程服务器的工作在对应证据不在当前 checkout 时不标记完成。

## 当前阶段：M2 真实复杂工程基线 + M4 Desktop Beta 交付

| ID | 工作项 | 状态 | 核验事实 |
|---|---|---|---|
| MA-001 | 独立 Mythos Agent 产品身份 | 完成 | `@mythos/agent-product` 版本 0.1.1，固定 DSH 0.1.0-rc.8、标签提交与官方上游 |
| MA-002 | Headless、Web 与交互式 CLI | 完成 | 源码/发布路径分离，Web 固定 loopback，交互式 CLI 通过 session id 恢复后续轮次 |
| MA-003 | Profile 与 Agent Preset 同一性 | 完成 | 验证 Bundle、单模型、persona、动态受控网关、系统 Preset 根与禁用 UI |
| MA-004 | 单轮评测基础设施 | 完成 | 5 个 release 用例 + 5 个 product 用例，均使用隔离 setup 和外部 verifier |
| MA-005 | 多轮与高级旅程基础设施 | 完成 | 冷恢复、压缩连续性和并行 subagent 三类旅程已有独立验收 |
| MA-006 | 真实 DSH 仓库评测 | 进行中 | 2 个固定修订用例已实现；`gate:real-repo-scope` 只强制 `dsh-session-seed-identity` |
| MA-007 | 运行证据与离线快照 | 完成 | 执行入口、Git tree、lockfile、配置、模型身份、计费与原始会话可归档并离线重验 |
| MA-008 | 付费评测 cohort 刷新 | 待办 | 当前实际开发 checkout 中没有 `flywheel/data/comparison.json`，不声明当前配置样本通过率 |
| MA-009 | 服务端飞轮数据 | 进行中 | 导入、健康分析与 `server-gate` 已实现；当前不在 `release:verify` 中 |
| MA-010 | 自包含确定性发布包 | 完成 | 固定 lockfile 离线构建、生产依赖闭包、绝对构建路径扫描和两个独立 staging 字节比较 |
| MA-011 | 发布完整性与消费端验证 | 完成 | `release-integrity.json` 覆盖普通文件/符号链接，仓外消费端验证 Headless、Web、M3 工具回路、交互恢复与篡改拒绝 |
| MA-012 | 发布门禁聚合 | 进行中 | `release:verify` 已包含产品测试、类型、Web、飞轮、五类评测门禁、打包与消费端；尚缺 `server-gate` 和第二个 real-repo case |
| MA-013 | MYTHOS Desktop Beta 产物 | 完成 | Electron `0.2.0-beta.6` 的 Developer ID 签名、Notarization、DMG/ZIP、严格签名、Stapler、Gatekeeper、完整性与 Host/Web 启动均已验收；GitHub Release 已公开 |
| MA-014 | Desktop 受控服务路由 | 完成 | 小型 HTTPS endpoint 列表、不含 userinfo、顺序健康探测与确定回退已有测试 |
| MA-015 | Desktop 签名热配置 | 完成 | Ed25519 签名 manifest、文件哈希/大小、staging 与原子 pointer 激活已有测试 |
| MA-016 | Desktop 应用更新 | 完成 | Beta 通道、主备转移、并发合并与禁止降级已有测试；签名 `0.2.0-beta.5` 在主源 404 后从 GitHub 后备源发现、下载、原地安装并启动 `0.2.0-beta.6`，更新后再次通过签名、Stapler、Gatekeeper 与 HTTP 200 健康检查 |
| MA-017 | MYTHOS 总控决策内核 | 完成 | 宪法策略、决策、原子记忆、阶段复审和 watchdog 已有测试 |
| MA-018 | MYTHOS 总控运行时装配 | 待办 | `control/` 尚无产品启动入口或 Cordis 组合项，也没有真实 ProjectReviewSource/ControllerPort |
| MA-019 | 产品文档管理体系 | 完成 | 产品索引、愿景、架构、模块、路线图、进度、开发规则和质量门禁已建立 |
| MA-020 | Desktop Runtime 归档与原子激活 | 待办 | 当前 App 内嵌 Runtime 为 28,823 个普通文件、3,292 个链接、约 297 MB；目标是以带哈希签名的归档随 App 分发，首次启动校验后原子解压到 Application Support 的版本目录 |

## 当前量化

MA-001～MA-020 共 20 项：14 项完成、3 项进行中、0 项阻塞、3 项待办。按工作项数量完成 70%；该数字只表示看板分解，不表示产品可公开发布。

| 剩余包 | 对应工作项 | 当前边界 | 验收结果 |
|---|---|---|---|
| 当前模型证据 | MA-006、MA-008、MA-012 | 机制与历史产物存在，当前 checkout 无 cohort 数据 | 当前版本/配置的 release、comprehensive、journey、advanced 和两个 real-repo case 均满足样本门禁 |
| 服务端数据闭环 | MA-009、MA-012 | 导入、分析与独立门禁存在 | 明确其发布决策作用，纳入门禁或记录不纳入的决策依据 |
| Desktop Runtime 精简 | MA-020 | `0.2.0-beta.6` 已交付，但自包含 Runtime 让 App Bundle 含约 2.9 万个小文件，签名和安装成本偏高 | 归档内置 Runtime，首次启动完成哈希校验、原子解压、并发启动和损坏恢复验收 |
| 总控装配 | MA-018 | 只有可测内核 | 真实项目状态可触发决策，指令可审计地到达 Lead，总控无执行工具 |

## 下一执行顺序

1. 推进 MA-020：先实现 Runtime 归档的确定性构建、哈希校验与 Application Support 原子激活，再验证首次启动、重复启动、并发启动、损坏恢复和应用升级。
2. 刷新当前产品身份的付费评测 cohort，分析两个真实仓库用例并统一发布覆盖。
3. 决定 `server-gate` 在发布中的强制性，将决定体现在 `release:verify`、测试与文档中。
4. 定义总控的真实状态源与 Lead 端口，先完成一次只读阶段复审，再开放暂停/停止操作。

## 证据来源

- 版本和 DSH 固定：[`package.json`](../package.json)。
- 评测用例与运行证据：[`eval/`](../eval/)。
- 飞轮与门禁：[`flywheel/`](../flywheel/) 与 [`release/verify.ts`](../release/verify.ts)。
- 发布闭包与消费端：[`release/`](../release/)。
- Desktop 实现与测试：[`../../../apps/desktop/`](../../../apps/desktop/)。
- 本地执行数据与产物：被 [`.gitignore`](../.gitignore) 排除的 `runs/`、`flywheel/data/`、`products/mythos-agent/dist/` 与 `apps/desktop/dist/`。

## 更新约定

- 开始一个看板工作项前将其标记为“进行中”。
- 只有实际运行验收命令并记录结果后才可标记为“完成”。
- 失败或阻塞项必须记录具体失败用例、解除条件和证据路径，不只写“待优化”。
- 同一次产品更改必须同步更新本看板，但评测统计仍以生成的 `comparison.json` 为数值真源。
