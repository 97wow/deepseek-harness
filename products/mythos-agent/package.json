{
  "name": "@mythos/agent-product",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "eval": "tsx eval/run.ts",
    "eval:repeat": "tsx eval/repeat.ts",
    "flywheel:analyze": "tsx flywheel/analyze.ts",
    "flywheel:build": "tsx flywheel/build.ts",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --ignoreConfig --noEmit --target ES2023 --module NodeNext --moduleResolution NodeNext --types node eval/run.ts eval/repeat.ts eval/cases.ts eval/options.ts eval/session-metrics.ts flywheel/analysis.ts flywheel/analyze.ts flywheel/archive.ts flywheel/build.ts"
  }
}
